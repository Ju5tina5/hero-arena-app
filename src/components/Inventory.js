import React from 'react';
import {Link} from 'react-router-dom';
import {useLocation} from "react-router-dom";

const Inventory = () => {


    return (
        <div className={'inventory flex-grow-3'}>
          <h3>Inventory</h3>
        </div>
    );
};

export default Inventory;